{
  "name": "@alexandria/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React components for Alexandria web surfaces. Components currently live in apps/web/src/components and graduate here once a second web surface (e.g. the docs site) consumes them.",
  "main": "src/index.ts",
  "scripts": {
    "lint": "echo 'ui: nothing to lint yet'"
  }
}
